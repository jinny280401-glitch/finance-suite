# Finance Suite — unified dependencies (production + MCP)
# Updated: 2026-09-05

# ── Web Framework (Production) ──
fastapi==0.115.0
uvicorn[standard]==0.30.0
jinja2==3.1.4
python-multipart==0.0.9
flask>=3.0.0

# ── Database & Auth (Production) ──
sqlalchemy==2.0.35
pyjwt==2.9.0
bcrypt==4.2.0

# ── MCP Server Core ──
mcp>=1.27.0,<2.0.0

# ── Financial Data Sources ──
akshare>=1.18.0
tushare>=1.4.0
jqdatasdk>=1.9.8

# ── HTTP & Networking ──
httpx==0.27.2
requests>=2.32.0
python-dotenv==1.0.1

# ── Caching ──
cachetools>=5.3.0

# ── Configuration ──
pyyaml>=6.0

# ── Data Processing ──
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.0

# ── PDF Export (Production) ──
reportlab>=4.0.0
weasyprint>=62.0

# ── LLM & Search ──
# (API key based, no pip package needed for Qwen/Tavily/Brave)

# ── Tests ──
pytest>=8.0.0

# ── Optional ──
youtube-transcript-api>=0.6.0
