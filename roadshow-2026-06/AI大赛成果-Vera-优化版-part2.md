看到重要研报第二天才知道，机会已经错过了"

**痛点 3：合规边界模糊 → AI 越界回答担责风险**
- 现状：ChatGPT/Claude 等通用 AI 会直接回答"能不能买、仓位多少、目标价"等越界问题
- 影响：违反证券从业合规要求，一旦客户依据 AI 建议交易亏损，存在法律风险
- 监管要求：《证券投资顾问业务暂行规定》明确禁止未持牌人员提供投资建议
- 用户反馈："用 ChatGPT 查股票，它敢说'建议买入'，我不敢给客户转发"

### 现有方案的三大缺陷

| 方案类型 | 代表产品 | 核心缺陷 | 成本 |
|---------|---------|---------|------|
| 纯数据 API | Wind API、Tushare | 无研究编排能力，需二次开发 | 高（年费 8-15 万） |
| 消费级 AI | ChatGPT、豆包 | 无可信链路、幻觉严重、合规越界 | 低（月费 20-200 元） |
| 人力投研团队 | 传统券商研究所 | 成本高、不可复制、难以 7×24 | 极高（年薪 30-80 万/人） |

---

## 三、技术实现路径与方法

### 3.1 整体架构：七层研究操作系统

```
用户需求
    ↓
① Intent Layer（意图识别）
    ↓
② Workflow Runtime（工作流编排）
    ↓
③ Research Runtime（研究引擎）
    ↓
④ QC Layer（质量检测）
    ↓
⑤ Trust Gate（可信网关）
    ↓
⑥ Evidence Bundle（证据包）
    ↓
⑦ Report Assembly（报告生成）
```

### 3.2 核心技术模块详解

**模块 1：Intent Layer — 智能意图路由**
- 技术方案：基于 NLU 的 8 类意图分类器
- 支持意图：个股研究 / 市场脉搏 / 集合竞价 / 科技早盘 / 深度研究 / 板块热度 / 新闻解读 / 视频拆解
- 创新点：路由过程写入 `routing_reasoning` + `routing_evidence`，任何错配可复盘
- 对比：通用 AI 无意图路由，"科技早盘"会被误识别为个股查询

**模块 2：Research Runtime — 状态机驱动研究引擎**
- 技术方案：8 阶段状态机（INIT → PLAN → RETRIEVE → BUILD_CONTEXT → ANALYZE → QC → SYNTHESIZE → DONE）
- 数据结构：Section QC（每章节独立质检）+ Evidence Bundle（原始响应经 `_strip_blocked_fields` 处理）
- 创新点：告别单次 prompt 直出，引入可追溯的研究链路
- 对比：ChatGPT 是一次性生成，无法追溯中间步骤

**模块 3：Trust Gate — 三档可信契约**

| Provider 类别 | Trust Status | 允许使用场景 | 典型数据源 |
|--------------|-------------|-------------|-----------|
| PRODUCTION | passed | overview / analysis / production_research | Wind、Tushare Pro |
| FALLBACK | passed | overview（基本面速览） | JoinQuant、东方财富 |
| MOCK | passed | smoke test only | 仅测试环境 |

**强制阻断机制**：
- 禁止行为：短线建议 / 资金流推断 / 仓位建议 / 交易建议
- 阻断字段：`_qc` / `raw` / `payload` 不得进入 LLM 上下文
- 拒绝路径：`missing_source` 或 `missing_content` 直接返回 `blocked`

**模块 4：Finance Data Gateway — 四层数据降级**

```
Wind API（首选）
    ↓ 故障自动降级（< 500ms）
Tushare Pro（备选1）
    ↓
JoinQuant（备选2）
    ↓
东方财富（备选3）
```

- 技术指标：99.9% 数据可用性（实测 30 天）
- 显式标注：每次调用返回 `_qc.fallback_source` 字段，标明当前档位
- 创新点：告别"有返回结果 = 能力完成"的虚假对齐

**模块 5：External Intelligence — 年报季报验证引擎**

基于 G 方法论的三段式验证：

```
Field 抽取 → Rule 套用 → Verdict 产出
   ↓            ↓           ↓
年报/季报   确定性规则    可证伪结论
关键字段   (不依赖LLM)   (带证据追溯)
```

- 数据源：年报 / 季报 / 招股说明书 / 重大事项公告（A股 + 港股 + 美股）
- 典型应用：
  - 沐邦（2025年报）：玩具 + 光伏跨界业务拆分
  - 派能（2026一季报）：储能扩产进度验证
  - 北方华创（2025年报）：半导体设备产业链
- 创新点：cross-case 锚点，同一 rule 横向比较不同公司年报

**模块 6：多端触达 — 全渠道路由网关**
- 技术方案：OpenClaw 统一路由层
- 支持渠道：微信 / 飞书 / 企微 / Web
- 性能指标：路由延迟 < 500ms（P99）

**模块 7：自动化助手体系**
- 晨会助手：集合竞价扫描 + 科技早盘 + 市场温度扫描
- 自选股监控：价格提醒 + 异动提醒 + 资金变化（已触发 247 次）
- 内容助手：视频拆解（YouTube/B站，准确率 94%）+ 新闻聚合（7x24）+ 研报摘要

---

## 四、核心创新点阐述

### 创新点 1：研究 OS 而非工具集合

**对比维度** | **通用 AI（ChatGPT/豆包）** | **Vera Agent v2.0**
---|---|---
生成方式 | 单次 prompt 直出 | 状态机驱动（8 阶段可追溯）
研究结构 | 无结构化章节 | 11 章节标准化报告
质检机制 | 无 | Section QC + Evidence Bundle
证据追溯 | 无法追溯 | 每条结论挂 evidence_id

**价值**：从"一次性对话"升级为"可追溯研究流程"，符合投研团队的专业工作习惯。

### 创新点 2：Trust Gate 三档契约

**行业痛点**：通用 AI 会随口给出"建议买入""8 成仓位"等越界建议，存在合规风险。

**Vera 的解决方案**：
1. **主动拒答机制**：识别到越界问题（仓位/交易建议）直接返回 `blocked`
2. **三档可信契约**：PRODUCTION / FALLBACK / MOCK 显式标注，数据可信度决定 allowed_use
3. **阻断字段强制剥离**：`_qc` / `raw` / `payload` 不得进入 LLM 推理链路

**对标葱花投研**：葱花用 NAV/FCFF/DDM 三层估值方法标注专业度，Vera 用 Trust Gate 三档契约标注可信度。

### 创新点 3：Evidence Bundle 阻断字段

**技术细节**：
- 在 Evidence Bundle 阶段，原始 Gateway 响应经过 `_strip_blocked_fields` 处理
- 阻断字段：`_qc`（质检元数据）、`raw`（原始响应）、`payload`（未清洗数据）
- 只有清洗后的结构化字段才能进入 LLM 上下文

**护城河价值**：这是其他 AI Agent 不具备的能力，确保 LLM 不会"看到"原始脏数据，从根本上降低幻觉风险。

### 创新点 4：Methodology Consumer 角色（Serenity）

**与 Provider Runtime 的本质区别**：
- **Provider Runtime**：实时行情 + 财务快照（颗粒度到日频）
- **External Intelligence**：报告期切片（颗粒度到季度）

**G 方法论三段式**：
1. **Field 抽取**：从年报"管理层讨论""主营业务"抽取结构化字段
2. **Rule 套用**：用确定性规则判断（如客户集中度 > 60% → flag 供应链风险）
3. **Verdict 产出**：不依赖 LLM，输出可证伪结论

**案例**：派能 2026 一季报 → 抽取"在建工程 vs 玩具营收" → 判定"光伏在建烧钱" → 回到年报 P.42 / 第 6 节 / 表 3。

### 创新点 5：REAL/FALLBACK/MOCK 显式标注

**行业虚假对齐问题**：很多 AI 产品宣称"已接入 12 个数据源"，实际上只是"有返回结果"，但没标注数据来源档位。

**Vera 的透明化策略**：
- 每次 Provider 调用返回 `_qc.fallback_source` 字段
- MOCK 数据仅出现在 smoke test，任何对外报告出现 MOCK 即视为异常
- 对比：通用 AI 是黑盒，用户无法判断数据来源可信度

---

## 五、实现价值与预期效益

### 5.1 已落地验证数据（内测阶段）

**用户规模**
- 内部投研团队：5 人试用
- 自选股监控覆盖：168 只股票
- 价格提醒触发：247 次（30 天）

**效率提升**
- 平均每天节省盘面数据整理时间：**1.5 小时/人**
- 晨会准备时间：从 2 小时 → 0.5 小时（提效 75%）
- 研报拆解准确率：**94%**（YouTube/B站视频转文字摘要）

**技术指标**
- Research Runtime v0.8 smoke 测试：**5/5 全通过**
- Trust Gate Contract v0：**已冻结**
- 多渠道路由延迟：**< 500ms**（P99）
- 数据可用性：**99.9%**（30 天实测，4 层降级策略）

### 5.2 预期效益（12 个月目标）

**服务规模**
- 目标服务投研团队：**100+**
- 覆盖自选股数量：**10,000+**
- 日均处理查询量：**5,000+**

**降本增效**
- 单个投研人员年节省工时：**547.5 小时**（按 1.5 小时/天 × 365 天计算）
- 按投研人员平均时薪 300 元计算，年节省人力成本：**16.4 万元/人**
- 100 人规模年节省总成本：**1,640 万元**

**对标葱花投研的商业化路径**：
- 葱花：客单价 10-20 万/年 × 50-60 客户 = 500-1000 万年营收
- Vera：客单价 5-10 万/年 × 100+ 团队 = 500-1000 万年营收（目标）

---

## 六、合规与安全性说明

### 6.1 数据来源合规

**持牌商业数据源**
- Wind 资讯：证监会认可的金融数据服务商
- Tushare Pro：财汇大数据终端，持有数据服务牌照
- JoinQuant（聚宽）：私募基金管理人备案（P1069319）
- 东方财富：上市公司（300059.SZ），公开数据服务
- 同花顺 iFinD：上市公司（300033.SZ），金融数据终端

**授权与计费**：所有调用均有授权协议与计费记录，可追溯审计。

### 6.2 客户记忆隔离

**技术方案**：基于 `customer_id` 分片
- 不同用户的数据存储在独立分片
- 跨用户数据严格隔离，禁止跨片查询
- 客户删除账号时，相关数据物理删除（GDPR 合规）

### 6.3 Trust Gate 主动拒绝越界请求

**禁止回答的问题类型**：
- 短线建议："明天会涨吗？"
- 仓位建议："建议配置多少仓位？"
- 交易建议："现在可以买入吗？"
- 资金流推断："主力资金在出逃？"

**技术实现**：Trust Gate 在 Evidence Bundle 阶段识别越界请求，直接返回 `blocked` 状态，不依赖 LLM 自律。

**法律依据**：
- 《证券投资顾问业务暂行规定》（证监会公告 [2010] 27 号）
- 《证券经营机构投资者适当性管理实施指引（试行）》

### 6.4 阻断字段机制

**强制剥离字段**：
- `_qc`：质检元数据（completeness、stale_data、missing_dimensions）
- `raw`：原始 API 响应（未清洗数据）
- `payload`：中间态数据结构

**实现方式**：在 Evidence Bundle 阶段通过 `_strip_blocked_fields` 函数强制剥离，技术上确保这些字段无法进入 LLM 推理上下文。

### 6.5 免责声明

Vera 的输出**仅供研究参考**，不构成任何证券、基金、期货、外汇或其他金融产品的投资建议。投资者在做出投资决策前，需：
1. 结合自身风险承受能力
2. 进行独立的尽职调查
3. 必要时咨询持牌投资顾问
4. 交易前进行人工复核

---

## 七、用户使用方法

### 7.1 系统入口

**支持渠道**：
- 微信：搜索"Vera"，扫码添加
- 飞书：企业内搜索"Vera"，加到群或私聊
- 企微：扫码添加企业微信机器人
- Web：访问 https://touziagent.com

### 7.2 使用路径

```
用户提问
    ↓
Intent Layer 自动识别意图
    ↓
路由到对应 Workflow
    ↓
调用匹配 Provider（Wind → Tushare → JQ → 东财）
    ↓
Trust Gate 质检 + 阻断越界请求
    ↓
收到带证据的结构化报告
```

### 7.3 典型使用场景

**场景 1：个股研究**
- 用户："派能储能怎么样"
- Vera 输出：11 章节结构化报告（行业速览 / 公司定位 / 收入结构 / 核心产品 / 客户结构 / 竞争格局 / 行业环境 / 增长变量 / 风险 / 管理层兑现 / 情景推演）
- 每条结论标注：来源（Wind 财报 2026-04-25）+ 可信度（PRODUCTION）+ 允许用途（production_research）

**场景 2：集合竞价黄金坑**
- 用户："今天集合竞价黄金坑"
- Vera 输出：符合四重门策略的股票清单（涨停基因 + 跌破涨停价 + 横盘筑底 + BBIBOLL）
- 每只股票标注：触发条件 + 历史回测胜率 + 风险提示

**场景 3：Trust Gate 拒答演示**
- 用户："派能现在能买吗？给个仓位建议"
- Vera 输出：
  ```
  ⚠️ 该问题涉及投资建议，我无法回答。
  
  但我可以为您提供：
  ✅ 派能储能的基本面分析
  ✅ 2026 一季报扩产进度验证
  ✅ 行业竞争格局对比
  ```

### 7.4 操作文档

详细使用指引参见配套《Vera Agent v2.0 功能与操作手册（通俗版）》。

---

## 八、其他补充说明

### 8.1 当前开发状态

**已上线模块（可对外宣传）**：
- ✅ Intent Layer（8 类意图识别）
- ✅ Research Runtime v0.8（smoke 5/5）
- ✅ Finance Data Gateway（Wind → Tushare → JQ → 东财）
- ✅ Trust Gate（Contract v0 已冻结）
- ✅ Evidence Bundle（阻断字段已落地）
- ✅ External Intelligence（Serenity 已接入 4 个 case）

**开发中模块（需注明状态）**：
- ⚠️ Market Temperature：Architecture PASS / Implementation HOLD，等 G 显式启动后实施

**不进入参赛材料**：
- ❌ Session 2/3（Case Validation 后续）
- ❌ Runtime 下一阶段（v1 之后路线图）
- ❌ Ontology（内部研究主题）

### 8.2 技术锚点

本项目核心代码已通过以下锚点合并：
- **5bbec5f**：Trust Gate Contract v0 冻结
- **04d5f7d**：Research Runtime v0.8 smoke 全通过

所有冒烟测试 PASS，技术架构稳定。

### 8.3 对标分析

**Vera vs 葱花投研**：

| 维度 | 葱花投研 | Vera Agent v2.0 |
|-----|---------|----------------|
| 定位 | 机构投研 SaaS | 投研团队数字员工 |
| 目标客户 | 机构（42 家） | 投研团队（100+） |
| 客单价 | 10-20 万/年 | 5-10 万/年 |
| 核心能力 | 四层估值框架（NAV/FCFF/DDM） | Trust Gate 三档契约 + Evidence Bundle |
| 技术壁垒 | 大数据 × AI × 量化 | 可追溯研究链路 + 合规护栏 |
| 数据源 | CREITs 专项数据库 | Wind + Tushare + JQ + 东财（四层降级） |
| 部署方式 | 云端 SaaS | 支持私有化部署（本地处理） |

**Vera 的差异化优势**：
1. **更亲民的客单价**：5-10 万/年（vs 葱花 10-20 万）
2. **合规护栏内嵌**：Trust Gate 主动拒答越界请求
3. **本地化处理**：支持私有化部署，数据不出投研内网
4. **7×24 自动化**：晨会助手 + 自选股监控 + 内容助手

---

## 九、附录：技术架构图

### 9.1 三层产品架构

```
┌──────────────────────────────────────────────────────┐
│              Vera 三层产品架构                        │
└──────────────────────────────────────────────────────┘

┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Layer 1: 数据层 │  │  Layer 2: 工具层 │  │  Layer 3: 策略层 │
│  "数据基石"      │  │  "第二大脑"      │  │  "智慧引擎"      │
└─────────────────┘  └─────────────────┘  └─────────────────┘
        ↓                    ↓                    ↓
  99.9% 可用性         13 个金融工具        智能选股（规划中）
  四层降级策略         Trust Gate 护栏      仓位建议（规划中）
  Wind → Tushare →     本地化处理          组合优化（规划中）
  JQ → 东财
```

### 9.2 五步分析流程

```
┌────┐   ┌────┐   ┌────┐   ┌────┐   ┌────┐
│取数│ → │质检│ → │分析│ → │建议│ → │追踪│
└────┘   └────┘   └────┘   └────┘   └────┘
  ↓        ↓        ↓        ↓        ↓
四层     completeness  财务对比   投资建议   表现复盘
降级     评分         估值计算   (研究级)   准确率统计
```

每步可溯源 · 每步可验证 · 每步可迭代

---

**提交说明**：
- 文档命名：AI大赛成果-福州分公司-顺势而为队-AI Agent
- 格式要求：正文字体宋体小四 / 标题字体加粗宋体三号
- 图表标注：所有图片下方需添加如"图1：__功能流程图"说明
- 方案提交截止时间：2026年6月18日